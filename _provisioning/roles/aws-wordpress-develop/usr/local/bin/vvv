#!/bin/bash
#set -x
#set -e
#set -u

CWD=$(pwd)
SWD=$(cd $(dirname $0) && pwd)

# trap
trap 'echo -e "\nabort\n" ; exit 1' 1 2 3 15

# log view
tail -f /var/log/nginx/ap.access.log | awk -F'\t' '{ print $5 "\t" $11 "\t" $4 }'

exit 0
